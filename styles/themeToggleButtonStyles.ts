import { createStyleSheet } from 'react-native-unistyles';

export const themeToggleButtonStyle = createStyleSheet((theme) => ({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    padding: 5,
    marginRight: 10,
  },
}));
